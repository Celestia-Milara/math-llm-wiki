# 考研数学 LLM Wiki 工作协议

## 0. 优先级

当规则冲突时，按以下顺序执行：

1. 用户当前明确要求
2. 本文件 `CLAUDE.md`
3. `99_System/docs/`、`99_System/schemas/`、`99_System/prompts/`
4. 旧笔记中的历史写法

不要因为旧文件仍有旧路径、旧标签或旧字段，就继续沿用旧规则。

## 1. 系统定位

这个 Vault 是考研数学的 LLM 辅助学习闭环系统，不是数学百科，也不是单纯资料仓库。

核心目标：

- 学某一讲前，生成行动型学习指南。
- 做题后，分析错题和重点题，尤其分析用户自己的错误思路。
- 用户复述概念理解后，审查其准确性和做题风险。
- 把长期有价值的内容沉淀为 Wiki、Records、复习计划。

核心闭环：

```text
学习指南 -> 做题记录 -> 错题/重点题 Records
-> 错因分析 -> Wiki/题型/方法补充
-> 概念理解审查 -> 复习计划
```

## 2. Claude Code 与 DeepSeek API 分工

Claude Code 是工作环境和文件编排器：

- 读取、移动、创建、更新 Obsidian 文件。
- 维护目录结构、frontmatter、Dataview 查询、双链路径。
- 按模板生成可落地的 Markdown。
- 对 DeepSeek 输出进行结构化、校验和去重。

DeepSeek API 是接入 Claude Code 的数学推理模型：

- 生成数学解释、推导、错因分析、变式题。
- 给出概念理解审查初稿。
- 辅助判断题型、方法、易错点。

最终写入 Vault 的内容必须由 Claude Code 按本协议整理。DeepSeek 的输出不能原样无审查写入长期 Wiki。

## 3. 目录写入规则

只使用当前新目录：

```text
00_Inbox/        临时输入
01_Raw/          原始资料
02_Learning/     学习过程
03_Wiki/         永久知识
04_Records/      错题、重点题、概念审查
05_Outputs/      汇总报告
06_Templates/    模板
99_System/       规则、脚本、提示词、字段契约
```

禁止向旧目录写入：`00_Raw/`、`01_Wiki/`、`02_Output/`、`03_Daily/`、`04_Templates/`、`docs/`、`scripts/`。

## 4. 三个高频工作流

### 4.1 生成某讲学习指南

触发：用户说“学习第 N 讲”“生成第 N 讲学习指南”。

输出到：`02_Learning/Guides/YYYY-MM-DD_第N讲学习指南.md`

必须包含：

- 本讲目标
- 先修知识
- 知识地图
- 核心概念、定理、方法、题型
- 学习顺序
- 易错预警
- 做题检查清单

学习指南不是教材摘要，必须告诉用户“怎么学、怎么做题、怎么自查”。

### 4.2 分析错题/重点题

触发：用户记录做题情况、错题编号、错误思路。

输出到：

- 错题分析报告：`05_Outputs/MistakeAnalysis/`
- 错题 Records：`04_Records/Mistakes/`
- 重点题 Records：`04_Records/KeyProblems/`

必须优先分析用户的错误思路：

```text
错误思路原文 -> 错误发生点 -> 为什么错 -> 正确触发信号 -> 下次动作
```

只记录错题和重点题。不要把当天所有做过的题都写进 `04_Records/`。

### 4.3 审查概念理解

触发：用户问概念，并用自己的话复述理解。

流程：

1. 解释概念。
2. 审查用户复述的正确点和偏差点。
3. 指出会导致做题错误的风险。
4. 给出修正版理解。
5. 只有长期有价值时，写入 `04_Records/ConceptReviews/`。

长期有价值包括：理解有偏差、概念高频易错、影响做题、需要更新 Wiki。

### 4.4 更新复习记录

触发：用户完成错题重做、或者需要更新复习状态。

流程：
1. 找到对应的 `04_Records/Mistakes/` 或 `04_Records/KeyProblems/` 文件。
2. 更新 `复习状态`：
   - 重做正确 → `已纠正`
   - 重做仍错 → `已重做仍错`，下次复习日期 +3天
   - 已完全掌握 → `已掌握`
3. 在复习记录表格中追加一行（日期、动作、结果）。
4. 如果该题已连续正确两次以上，可以将其标记为 `已掌握` 并更新下次复习日期为空。

**间隔重复原则：**
- 首次错 → 1天后复习
- 重做仍错 → 3天后复习
- 重做正确 → 7天后复习
- 连续两次正确 → `已掌握`，不再提醒

### 4.5 生成薄弱环节诊断

触发：用户学完一讲、做完练习后，或主动说"薄弱环节诊断"。

流程：
1. 读取 `04_Records/Mistakes/` 中该讲的错题记录。
2. 按 `错因类型` 聚合统计，识别高频错因。
3. 按 `关联方法` 聚合，识别薄弱方法。
4. 生成诊断报告，写入 `05_Outputs/StageSummaries/YYYY-MM-DD_第N讲薄弱环节.md`。
5. 如果发现某方法/题型反复出错，提示是否需要建立 `03_Wiki/ProblemTypes/` 页面。

## 5. 建页克制规则

可以建页：

- `03_Wiki/Concepts/`：可展开 2 个以上子主题的核心概念。
- `03_Wiki/Theorems/`：常考定理、公式、推论。
- `03_Wiki/Methods/`：有明确适用条件和 SOP 的解题方法。
- `03_Wiki/ProblemTypes/`：可识别、可训练、可复盘的题型。
- `03_Wiki/Hubs/`：章节、专题、跨章节索引。
- `04_Records/`：错题、重点题、有价值的概念审查。

不要建页：

- 单一公式。
- 冷门或一次性结论。
- 纯临时问答。
- 已被现有 Wiki 充分覆盖的基础定义。
- 用户当天全部做题流水。

## 6. 数学内容质量规则

- 行内公式用 `$...$`，独立公式用 `$$...$$`。
- 多行推导用 `aligned`，必要时用 `\text{}` 标注步骤。
- 检查条件、定义域、端点、极限方向、参数范围。
- 检查等价变形、放缩方向、换元范围、积分上下限。
- 错题分析不要只给标准答案，要解释用户为什么会走错。
- AI 生成但未人工核查的长期内容必须标注 `AI状态: 待核查`。

## 7. 增量更新规则

- 不覆盖已有 Wiki 正文。
- 需要修正时，追加“修正说明”“易错补充”或新小节。
- 新记录优先用 `06_Templates/`。
- 新 Dataview 查询必须使用新目录路径。
- 写入前先搜索是否已有同主题页面，优先更新而不是新建。

## 8. 规则索引

- `99_System/docs/知识库结构.md`
- `99_System/docs/行为准则.md`
- `99_System/docs/快捷指令.md`
- `99_System/docs/章节映射维护.md`
- `99_System/docs/总结第N讲.md`
- `99_System/schemas/frontmatter字段规范.md`
- `99_System/prompts/deepseek调用规范.md`
