# 数学资料库工作协议

## 1. 优先级与边界

规则冲突时依次遵从：用户当前明确要求、`goal.md`、原始资料保护要求、本文件及 `99_System/` 当前规则、历史文件约定。

本 Vault 是 `kaoyan` 的独立数学资料库，不是学习闭环或复习管理系统。`D:\tool\Obsidian\kaoyan` 是个人复习阶段、每日推进、掌握证据、未闭环任务和周复盘的唯一正式来源；在本项目中对它只读，绝不写入、移动或同步其文件。

## 2. 允许与禁止的工作

允许：

- 保存和整理 `01_Raw/` 中的讲义、题目、解析、图片及其稳定定位；
- 维护资料索引、章节映射、Wiki 的来源、可信状态和链接；
- 新建有独立检索价值的通用数学参考页，并标为 `S3 待核查`，除非已人工确认；
- 修复本 Vault 内的路径、关系字段和校验脚本。

禁止：

- 在 `math` 新建每日学习日志、学习指南、活动错题、重点题、复习计划或进度仪表盘；
- 写入或推断用户掌握状态、当前进度、待重做/已纠正等个人状态；
- 把旧 Records、AI 判断或历史报告同步为 `kaoyan` 的正式事实；
- 静默改写、删除或覆盖 `01_Raw/` 原始资料正文；
- 为填补链接而批量创建没有独立价值的 Wiki 页面。

## 3. 内容与目录规则

- S0 原始资料保持原貌；S1 定位数据只服务定位；S2 是人工核查参考；S3 是待核查参考；L 是历史学习数据。
- `02_Learning/`、`04_Records/`、`05_Outputs/` 只读式保留历史产物。可补充历史说明或修复现有资料链接，但不得把其中状态描述为当前状态。
- 新资料索引写入 `01_Raw/`；新通用知识写入 `03_Wiki/`；维护规则和脚本写入 `99_System/`。
- 所有新 Wiki 页面必须包含 `标题`、`类型`、`来源`、`可信状态`。AI 整理内容默认 `S3 待核查`。
- 表示页面关系的 frontmatter 必须用 Obsidian Link，如 `关联概念: [[03_Wiki/Concepts/导数-Derivative]]`；不得使用仅包含路径的普通字符串。

## 4. 资料导航工作流

当用户询问某个资料或题目时：

1. 先查 `01_Raw/资料主索引.md` 及对应分科索引。
2. 高数讲次再查 `01_Raw/章节映射.json`，按文件和经校验的行范围定位。
3. 回答时给出 Vault 内稳定路径、科目、章节/讲次、题号（若有）和资料类型。
4. 如需让 `kaoyan` 记录入口，仅提供可复制的定位文本或 URI 示例；不修改 `kaoyan`。

## 5. Wiki 维护规则

- 不覆盖既有 Wiki 正文；修订时追加“核查说明”或局部更正。
- 可信状态与用户学习状态完全分离。不得使用 `掌握状态`、`问题类型`、`最后练习` 等个人状态字段。
- 链接优先使用完整 Vault 路径，且目标必须存在。无法可靠对应的历史链接应删除并在报告中记录，不创建虚构页面。
- 数学内容保持条件、定义域、端点、极限方向、参数范围及推导依据清楚。

## 6. 验证与可恢复性

- 批量修改前检查 Git 状态；保留用户已有变更。
- 运行 `python 99_System/scripts/check_vault.py` 后再交付。脚本只读；自动修复必须另行显式设计和授权。
- 任何迁移、修复与未解决问题都写入 `REBUILD_REPORT.md`。

## 7. 规则索引

- `99_System/docs/知识库结构.md`
- `99_System/docs/行为准则.md`
- `99_System/docs/资料库维护.md`
- `99_System/docs/章节映射维护.md`
- `99_System/schemas/frontmatter字段规范.md`
