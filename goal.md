# Math 数学资料库重构目标

> 创建日期：2026-07-17  
> 状态：待执行  
> 适用仓库：`D:\tool\Obsidian\math`  
> 外部协作 Vault：`D:\tool\Obsidian\kaoyan`（只读，不在本项目中修改）

## 1. 文档地位

本文件是 `math` 仓库后续重构的最高级目标、修改边界和验收依据。

规则发生冲突时，按以下顺序处理：

1. 用户当前明确要求。
2. 本文件 `goal.md`。
3. 用户已有原始数学资料及可恢复性要求。
4. 当前有效的 `README.md`、`CLAUDE.md` 和 `99_System/` 规则。
5. 旧目录、旧模板和旧笔记中的历史约定。

如果现有规则仍把 `math` 定义为独立的完整学习闭环，而与本文件的“数学资料库”定位冲突，应在后续重构中更新旧规则，不继续沿用冲突设计。

## 2. 总目标

将当前 `math` 仓库重构为：

> **服务于 `kaoyan` 考研复习 Vault 的独立数学资料库，负责保存、定位、检索和核查数学原始资料与通用参考知识，但不再维护个人复习进度、错题状态、每日记录或复习计划。**

重构后的两库关系为：

```text
math
  保存讲义、题目、解析、图片、章节映射和通用数学参考
  ↓ 提供资料定位与阅读入口
真实数学复习
  ↓ 形成推进事实、掌握证据和未闭环问题
kaoyan
  保存每日日记、当前主线、掌握证据、错题/模糊任务和周复盘
```

系统成功的关键不在于 `math` 中页面更多，而在于：

- 查找一讲、一题或一个数学主题时能够快速定位可靠资料；
- 原始资料与 AI 整理内容的可信等级明确；
- `math` 不再与 `kaoyan` 重复维护学习状态；
- 两个 Vault 可以稳定地人工导航，但不会形成双份任务和双份进度；
- 资料库维护成本明显低于它带来的检索价值。

## 3. 核心问题

本次重构必须解决以下问题：

1. `math` 同时承担资料库、学习过程、错题系统和复习仪表盘，职责过多。
2. `math` 与 `kaoyan` 都可能保存个人复习状态，存在双重事实来源风险。
3. Wiki 中存在断链、无效关联和未明确标记的 AI 生成内容。
4. 部分关联字段是普通字符串，但查询按 Obsidian Link 处理，导致反向查询失效。
5. 章节映射虽基本可用，但存在行号越界和仅覆盖部分科目的范围问题。
6. `01_Raw/` 同时包含高等数学、线性代数、概率论和综合卷，但当前章节模型主要按高数“第 N 讲”组织。
7. 旧学习指南、错题状态和输出报告容易被误认为仍在持续维护。
8. 规则、模板和脚本中存在新旧字段、新旧状态和新旧路径并存的问题。

后续修改不得只做目录美化，也不得通过继续批量生成 Wiki 页面掩盖这些结构性问题。

## 4. 两个 Vault 的权威边界

### 4.1 `math` 的权威内容

`math` 只对以下内容负责：

- 数学讲义、题目、解析、图片和其他原始材料；
- 原始材料的稳定路径、编号、来源和定位信息；
- 高等数学、线性代数、概率论、综合卷的资料索引；
- 经人工核查或明确标注状态的概念、定理、方法和题型参考页；
- 资料库自身的链接、schema、索引和校验脚本。

### 4.2 `kaoyan` 的权威内容

以下内容的唯一正式来源是 `kaoyan`，不得在 `math` 中建立第二套活动状态：

- 当前复习阶段、资料位置、主线和风险；
- 每日实际推进；
- 独立掌握证据和待验证内容；
- 未完成的 `#错题`、`#模糊` 等任务；
- 周复盘和下一阶段优先动作；
- 宏观复习进度。

### 4.3 修改权限

- 本重构只允许修改 `math` 仓库。
- `D:\tool\Obsidian\kaoyan` 在整个项目中只允许读取，不允许写入、移动、删除或改名。
- 如需让 `kaoyan` 增加到 `math` 的入口，只能在报告中给出建议或待用户执行项；除非用户以后单独明确授权，否则不得修改 `kaoyan`。
- 不得自动把 `math` 中的旧错题状态、AI 判断或掌握状态同步为 `kaoyan` 的正式事实。

## 5. 内容可信等级

资料库必须区分以下层级：

| 等级 | 内容 | 使用方式 |
|---|---|---|
| S0 原始资料 | 讲义、题目、解析、原图 | 数学内容的主要依据，保持原貌 |
| S1 定位数据 | 章节映射、题号、来源路径、索引 | 用于快速找到 S0，不替代原文 |
| S2 已核查参考 | 经人工确认的 Wiki 页面 | 可作为稳定参考 |
| S3 待核查参考 | AI 生成或尚未人工确认的 Wiki 页面 | 必须显式标记，不得伪装成已核查事实 |
| L 历史学习数据 | 旧学习指南、错题记录、复习报告 | 只作为历史档案，不再代表当前状态 |

“用户是否掌握”不属于内容可信等级，也不得保存在通用数学知识页中作为资料属性。

## 6. 目标目录职责

后续重构优先保留现有顶层目录，避免不必要的大规模搬迁；通过职责收缩和清晰标记完成转型。

```text
00_Inbox/          待整理的数学资料输入
01_Raw/            原始讲义、题目、解析、图片和资料索引
02_Learning/       历史学习产物；重构后不再写入活动学习状态
03_Wiki/           通用数学参考知识，必须有可信状态和有效链接
04_Records/        历史个人记录；重构后不再新增活动错题或复习状态
05_Outputs/        历史分析与仪表盘；重构后不再作为复习入口
06_Templates/      仅保留资料页和 Wiki 维护所需模板
99_System/         资料库规则、schema、索引规范和校验脚本
```

后续可以为 `02_Learning/`、`04_Records/`、`05_Outputs/` 增加清晰的历史说明或统一归档入口，但默认不得删除其现有内容。

## 7. 明确非目标

本次重构不负责：

- 在 `math` 中建立每日复习日记；
- 在 `math` 中维护当前学习进度或“第几讲学到哪里”；
- 为每道新错题创建独立活动笔记；
- 维护“待重做、已纠正、已掌握”等个人复习状态；
- 自动生成复习计划、学习打卡、连续天数或努力评价；
- 根据旧错题或 AI 输出更新 `kaoyan` 的正式状态；
- 把全部原始资料复制到 `kaoyan/01-数学/`；
- 为了追求知识图谱完整而批量创建低价值 Wiki 页面；
- 在没有人工核查时把 AI 内容标记为已核查；
- 在本次重构中修改 `kaoyan` 的文件、配置或任务。

## 8. 数据保护原则

1. 修改前必须检查 Git 状态并记录基线。
2. 不得静默删除、覆盖或改写 `01_Raw/` 中的原始资料正文。
3. 大规模移动、重命名或批量 frontmatter 修改前必须先形成可恢复点和迁移清单。
4. 发现已有用户修改时必须保留；无法安全绕开时停止并请求用户确认。
5. 旧学习记录和错题记录即使不再活动使用，也不得为了“结构干净”而删除。
6. 自动校验产生的测试文件不得留在正式资料目录。
7. 每个迁移、修复和未处理问题都必须记录在后续 `REBUILD_REPORT.md` 中。

## 9. 修改计划

后续实施必须按以下阶段推进。每个阶段完成后先验证，再进入下一阶段。

### 阶段 0：建立基线和可恢复点

目标：在任何批量修改前明确仓库真实状态。

计划：

1. 检查 Git 工作树、当前分支、最近提交和未跟踪文件。
2. 统计各目录文件数量、大小、文件类型和最近修改时间。
3. 记录原始资料、Wiki、历史记录和输出报告的数量。
4. 扫描全部内部链接、附件引用、来源路径和章节映射引用。
5. 输出修改前审计结果，列出断链、未知字段、重复页面和映射异常。
6. 确认可通过 Git 恢复；如不能，先建立仓库内或仓库外的可恢复备份方案。

阶段产物：基线审计记录和明确的可恢复方式。

### 阶段 1：更新仓库定位和规则

目标：让所有入口文件对仓库职责给出一致解释。

计划：

1. 更新 `README.md`，将项目定位改为数学资料库。
2. 更新 `CLAUDE.md`，删除或降级与活动复习状态冲突的工作流。
3. 更新 `99_System/docs/知识库结构.md` 和行为规则。
4. 明确 `kaoyan` 是个人复习状态的唯一来源，且在本项目中只读。
5. 给历史目录增加不误导用户的说明，但不删除历史内容。
6. 统一术语：原始资料、定位数据、参考知识、历史学习数据。

阶段产物：一致的入口文档和 AI 工作边界。

### 阶段 2：建立可靠的资料导航

目标：用户能够按科目、章节、讲次和题目快速找到原始资料。

计划：

1. 盘点高等数学、线性代数、概率论和综合卷的覆盖范围。
2. 将现有仅面向高数的“第 N 讲”模型扩展为：

   ```text
   科目 → 模块/章节 → 课程单元 → 资料类型 → 题号或行范围
   ```

3. 保留兼容的高数讲次入口，不破坏已有路径。
4. 修复章节映射中的越界行号、过期时间和范围说明。
5. 为原始资料建立一个主索引和必要的分科索引。
6. 索引只指向实际存在的文件，不生成虚假链接。
7. 为常用资料提供稳定、可复制的 Vault 内路径。

阶段产物：覆盖范围明确、路径有效的数学资料索引。

### 阶段 3：修复 Wiki 的可信度和链接完整性

目标：让 `03_Wiki/` 成为可用的辅助参考，而不是断链和未核查内容的集合。

计划：

1. 为所有 AI 生成页面补齐明确的 `AI状态` 或等价可信状态。
2. 将内容核查状态与用户掌握状态彻底分离。
3. 修复正文中的失效 Wikilink。
4. 修复错题历史记录中指向不存在概念、方法和题型的关联。
5. 将需要参与 Dataview 比较的关系字段转换为真正的 Obsidian Link 类型。
6. 统一页面文件名、标题、别名和英文后缀的解析规则。
7. 修复仍使用旧英文状态值、旧目录路径和旧类型值的页面或提示。
8. 不因修复关系而批量创建没有独立价值的新页面。

阶段产物：可追溯、可查询、可信状态明确的参考知识区。

### 阶段 4：降级旧学习闭环，消除双重状态源

目标：确保 `math` 不再与 `kaoyan` 竞争个人复习状态。

计划：

1. 将 `02_Learning/` 定义为历史学习产物，不再生成新的活动日记和学习指南状态。
2. 将 `04_Records/` 定义为历史记录，不再新增活动错题和复习状态。
3. 将 `05_Outputs/` 定义为历史报告，不再作为当前复习仪表盘。
4. 对旧 Dashboard、复习清单和弱点报告增加历史提示或移入明确归档入口。
5. 删除或停用会继续生成双份进度、任务和复习状态的模板规则。
6. 保留旧内容的来源、日期和历史意义，不伪造迁移后的当前状态。

阶段产物：单一学习状态源，旧记录仍可追溯。

### 阶段 5：建立与 `kaoyan` 的低耦合导航约定

目标：两个独立 Vault 可以互相定位，但不复制资料和状态。

计划：

1. 在 `math` 中提供稳定资料路径和跨 Vault URI 示例。
2. 约定 `kaoyan` 日记引用数学资料时至少记录：科目、讲次/章节、题号和资料路径。
3. 不要求 Dataview、反向链接或任务状态跨 Vault 自动同步。
4. 不在 `math` 中创建 `kaoyan` 日记、任务或状态的镜像。
5. 如需修改 `kaoyan` 增加资料入口，只在报告中列为用户确认后的独立任务。
6. 通过 Obsidian 手工验证至少三个常用数学资料的跨 Vault 打开行为。

阶段产物：简单、稳定、无双向同步负担的两库协作方式。

### 阶段 6：增加确定性校验

目标：让路径、映射和 schema 问题能够自动发现。

计划：

1. 增加只读链接检查器。
2. 增加 frontmatter/schema 检查器。
3. 增加章节映射 JSON 解析、文件存在和行号边界检查。
4. 增加 AI 状态缺失检查。
5. 增加旧目录、旧状态值和无效关系字段检查。
6. 所有脚本默认只读；任何自动修复模式必须单独显式启用。
7. 提供一条统一验证命令或清晰的验证步骤。

阶段产物：可重复运行的资料库健康检查。

### 阶段 7：端到端验收和交付

目标：证明仓库已经完成职责转型，而不是只完成文件修改。

计划：

1. 从资料主索引打开一份高数讲义、一份线代题目和一份概率解析。
2. 从章节映射定位至少三个不同章节的题目和解析。
3. 从 Wiki 页面跳转到真实来源，验证可信状态可见。
4. 验证旧学习记录仍存在但不再被描述为当前状态。
5. 验证当前规则不会在 `math` 中生成新的每日任务、复习状态或进度。
6. 验证整个执行过程没有修改 `kaoyan`。
7. 生成 `REBUILD_REPORT.md`，逐项报告验收结果和未验证项。

阶段产物：可审查的重构报告和手工验收步骤。

## 10. P0 验收标准

以下 P0 项全部通过后，才允许声明“资料库重构完成”。如果存在待手工验收或未通过项，总体状态只能写“部分完成”。

### 10.1 数据安全

- [ ] **P0-01** 修改前已记录 Git 状态和可恢复方式。
- [ ] **P0-02** `01_Raw/` 原始资料没有被静默删除、覆盖或错误改写。
- [ ] **P0-03** 旧学习记录、错题记录和输出报告没有因职责降级而丢失。
- [ ] **P0-04** 所有移动、重命名、批量修改和归档均记录在 `REBUILD_REPORT.md`。
- [ ] **P0-05** 校验或验收产生的测试文件没有残留在正式资料目录。

### 10.2 职责边界

- [ ] **P0-06** `README.md` 明确将 `math` 定义为数学资料库，而不是独立复习管理系统。
- [ ] **P0-07** `CLAUDE.md` 和 `99_System/` 不再要求在 `math` 中维护当前个人复习状态。
- [ ] **P0-08** 文档明确 `kaoyan` 是复习阶段、每日记录、未闭环任务和周复盘的唯一来源。
- [ ] **P0-09** 后续工作流不会在 `math` 中创建第二份活动错题任务、学习进度或复习计划。
- [ ] **P0-10** 整个重构过程中没有修改 `D:\tool\Obsidian\kaoyan` 中的任何文件。

### 10.3 原始资料与定位

- [ ] **P0-11** 高等数学、线性代数、概率论和综合卷的资料覆盖范围有明确说明。
- [ ] **P0-12** 资料主索引中的所有链接均指向真实文件。
- [ ] **P0-13** 章节映射 JSON 可以正常解析。
- [ ] **P0-14** 章节映射中的所有文件路径均存在。
- [ ] **P0-15** 章节映射中的起止行号不超出目标文件范围。
- [ ] **P0-16** 随机抽查至少三个讲次时，可以从索引定位讲义、题目和解析。
- [ ] **P0-17** 原始资料与 AI 整理内容在界面和文档中可以明确区分。

### 10.4 Wiki 可信度与关系

- [ ] **P0-18** 所有已知 AI 生成的长期参考页均有明确可信状态。
- [ ] **P0-19** 内容核查状态不再与用户掌握状态混用。
- [ ] **P0-20** 活动资料导航和核心 Wiki 页面不存在未说明的断链。
- [ ] **P0-21** 关联概念、方法和题型的字段类型与 Dataview 查询契约一致。
- [ ] **P0-22** 不存在指向虚构页面的活动关联；历史无效关联已修复或明确列入报告。
- [ ] **P0-23** 旧英文状态值、旧目录路径和当前 schema 的冲突已修复或明确隔离。

### 10.5 历史区域

- [ ] **P0-24** `02_Learning/`、`04_Records/`、`05_Outputs/` 的历史性质有清晰说明。
- [ ] **P0-25** 旧仪表盘不会被误认为当前复习入口。
- [ ] **P0-26** 旧错题的“待重做”等状态不会被表述为 `kaoyan` 当前任务。
- [ ] **P0-27** 历史记录仍能追溯到原始日期和来源。

### 10.6 两库导航

- [ ] **P0-28** 文档给出正确的 `obsidian://open?vault=math&file=...` 使用说明。
- [ ] **P0-29** 至少三个跨 Vault 资料入口经过 Obsidian 手工点击验证。
- [ ] **P0-30** 两库协作不依赖 Dataview 跨 Vault 查询或自动双向同步。
- [ ] **P0-31** `kaoyan` 无需复制整套 `math` 原始资料即可记录稳定资料定位。

### 10.7 自动校验与维护

- [ ] **P0-32** 存在可重复运行的内部链接检查。
- [ ] **P0-33** 存在可重复运行的 frontmatter/schema 检查。
- [ ] **P0-34** 存在可重复运行的章节映射文件和行号边界检查。
- [ ] **P0-35** 校验脚本默认只读，不会在检查过程中修改资料。
- [ ] **P0-36** README 或维护文档能在五分钟内解释资料查找、可信状态和两库边界。

### 10.8 交付

- [ ] **P0-37** `REBUILD_REPORT.md` 包含最终目录职责、修改文件、迁移记录和测试结果。
- [ ] **P0-38** 所有未通过或未验证项目均说明原因、影响和精确手工步骤。
- [ ] **P0-39** Git 工作树中的最终变更仅包含本次重构范围内的文件。
- [ ] **P0-40** 用户能够从 `kaoyan` 的数学复习上下文定位 `math` 资料，同时所有个人状态仍只保存在 `kaoyan`。

## 11. P1 可选改进

P0 完成后，可以按实际使用摩擦选择性实施：

- 为常用讲次生成跨 Vault URI 清单；
- 为题目建立稳定 `problem_id`，避免路径变化影响引用；
- 为高频资料增加别名和搜索关键词；
- 为 Wiki 建立按科目和主题的轻量索引；
- 增加重复页面和相似主题检测；
- 为原始图片增加来源页和题号元数据；
- 在不修改 `kaoyan` 的前提下生成可复制的 Markdown 引用片段。

P1 不得重新引入个人进度、错题状态和复习调度。

## 12. 完成定义

只有同时满足以下条件，才算完成本次重构：

1. `math` 的全部入口文档都把它描述为数学资料库。
2. `kaoyan` 明确且实际保持为个人复习状态的唯一来源。
3. 原始数学资料完整、可恢复且可通过稳定索引定位。
4. Wiki 的 AI 状态、链接和关系契约达到 P0 要求。
5. 历史学习数据得到保留，但不会继续产生或冒充当前状态。
6. 两库导航方式经过验证，且不依赖自动双向同步。
7. 校验脚本可以重复发现链接、schema 和映射问题。
8. `REBUILD_REPORT.md` 已生成并如实记录所有结果。
9. P0-01 至 P0-40 全部通过。
10. 没有修改 `kaoyan` 中的任何文件。

如果只能完成静态检查而无法运行 Obsidian 图形界面，必须把相关项目标记为“待手工验收”，不得声称已经完整通过。
